#include <stdio.h>

int main() {
        int = libra;
        float =  pes;
        double =  metro, kg;
        scanf("%d %f", &libras, &pes);
        kg = libra * 0.45;
        metro =  pes * 0.30;
        printf("Em quilos: %.2lf\n", kg);
        printf("Em metros: %.2lf", metro);
    return 0;
}